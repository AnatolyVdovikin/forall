#!/bin/bash
# Скрипт для запуска на Render
# Выполняется автоматически при деплое

echo "🚀 Запуск ForAll на Render..."

# Применяем миграции при первом запуске
if [ ! -f .migrated ]; then
  echo "🔄 Применение миграций БД..."
  npm run migrate
  touch .migrated
fi

# Запускаем сервер
echo "▶️  Запуск сервера..."
npm start
