# Деплой ForAll на Render.com (Бесплатно!)

## 🎯 Render.com - Идеальный выбор для бесплатного хостинга

**Render** предлагает:
- ✅ Бесплатный tier для веб-сервисов
- ✅ Бесплатная PostgreSQL БД
- ✅ Бесплатный Redis
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификаты автоматически
- ✅ Docker поддержка

## 🚀 Быстрый деплой (5 минут)

### Шаг 1: Подготовка GitHub репозитория

1. Создайте репозиторий на GitHub
2. Загрузите код:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/forall.git
git push -u origin main
```

### Шаг 2: Регистрация на Render

1. Перейдите на [render.com](https://render.com)
2. Зарегистрируйтесь через GitHub
3. Подключите ваш репозиторий

### Шаг 3: Создание БД (PostgreSQL)

1. В Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Настройки:
   - **Name**: `forall-db`
   - **Database**: `forall`
   - **User**: `forall_user`
   - **Plan**: **Free**
3. Нажмите **"Create Database"**
4. Скопируйте **Internal Database URL** (понадобится позже)

### Шаг 4: Создание Redis

1. В Dashboard нажмите **"New +"** → **"Redis"**
2. Настройки:
   - **Name**: `forall-redis`
   - **Plan**: **Free**
3. Нажмите **"Create Redis"**

### Шаг 5: Деплой Backend

#### Вариант A: Через render.yaml (Рекомендуется)

1. В Dashboard нажмите **"New +"** → **"Blueprint"**
2. Подключите ваш GitHub репозиторий
3. Render автоматически обнаружит `render.yaml`
4. Нажмите **"Apply"**
5. Все сервисы создадутся автоматически!

#### Вариант B: Вручную

1. В Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Настройки:
   - **Name**: `forall-backend`
   - **Environment**: `Node`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: **Free**

4. **Environment Variables**:
   ```
   NODE_ENV=production
   PORT=10000
   DB_HOST=<из PostgreSQL сервиса>
   DB_PORT=5432
   DB_NAME=forall
   DB_USER=<из PostgreSQL сервиса>
   DB_PASSWORD=<из PostgreSQL сервиса>
   REDIS_HOST=<из Redis сервиса>
   REDIS_PORT=6379
   JWT_SECRET=<сгенерируйте случайную строку минимум 32 символа>
   CORS_ORIGIN=https://your-app-name.onrender.com
   LOG_LEVEL=info
   ```

5. Нажмите **"Create Web Service"**

### Шаг 6: Применение миграций

После первого деплоя:

1. Откройте ваш Web Service
2. Перейдите в **"Shell"** вкладку
3. Выполните:
```bash
npm run migrate
```

Или через Render Dashboard → **"Shell"**:
```bash
cd /opt/render/project/src && node src/database/migrate.js
```

### Шаг 7: (Опционально) Тестовые данные

```bash
npm run seed
```

## 🔧 Настройка переменных окружения

В Render Dashboard для вашего Web Service:

### Обязательные переменные:
- `NODE_ENV=production`
- `PORT=10000` (Render использует этот порт)
- `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` (из PostgreSQL)
- `REDIS_HOST`, `REDIS_PORT` (из Redis)
- `JWT_SECRET` (сгенерируйте: `openssl rand -hex 32`)
- `CORS_ORIGIN` (ваш Render URL)

### Опциональные:
- `LOG_LEVEL=info`
- `STORAGE_TYPE=local`
- `STORAGE_PATH=./uploads`

## 📝 Важные замечания

### Render Free Tier ограничения:
- ⚠️ Сервисы "засыпают" после 15 минут неактивности
- ⚠️ Первый запрос после сна может занять 30-60 секунд
- ⚠️ 750 часов бесплатного времени в месяц
- ⚠️ 512MB RAM
- ⚠️ Медиа файлы хранятся локально (не персистентно)

### Решения для production:

1. **Для медиа файлов**: Используйте S3 или Cloudinary
2. **Для предотвращения сна**: Используйте uptime monitoring (UptimeRobot)
3. **Для большего трафика**: Upgrade до Paid plan ($7/месяц)

## 🔗 Полезные ссылки

- [Render Dashboard](https://dashboard.render.com)
- [Render Docs](https://render.com/docs)
- [Render Free Tier](https://render.com/docs/free)

## 🆘 Troubleshooting

### Сервис не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте, что порт = 10000

### БД недоступна
- Убедитесь, что используете Internal Database URL
- Проверьте, что БД создана и запущена

### Медленный первый запрос
- Это нормально для Free tier (сервис просыпается)
- Используйте uptime monitoring для поддержания активности

## ✅ После деплоя

1. Проверьте health endpoint: `https://your-app.onrender.com/health`
2. Примените миграции через Shell
3. Обновите CORS_ORIGIN на ваш реальный домен
4. Настройте мобильное приложение на новый API URL

## 🎉 Готово!

Ваше приложение будет доступно по адресу:
`https://forall-backend.onrender.com`

API endpoints:
- `https://forall-backend.onrender.com/api/auth/register`
- `https://forall-backend.onrender.com/api/challenges/swipe`
- и т.д.
